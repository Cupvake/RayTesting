const { RichEmbed } = require("discord.js");
const Discord = require('discord.js');

module.exports = {
    name:"uno",
    category:"fun",
    description:"Sends an uno reverse card. No u!",
    run: async (client, message, args) => {

        //CODE IS IN THE INDEX
        
    }
}