const { RichEmbed } = require("discord.js");

module.exports = {
    name:"whois",
    aliases: ["who", "user", "info"],
    category:"info",
    description: "Returns user information",
    run: async (client, message, args) => {
        
        //CODE IS IN THE WHOIS.JS

    }
}